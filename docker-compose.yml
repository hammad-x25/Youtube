services:

  backend:
    build:
      context: ./Backend
    container_name: youtube-backend
    env_file:
      - ./Backend/.env
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./Frontend
    container_name: youtube-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend